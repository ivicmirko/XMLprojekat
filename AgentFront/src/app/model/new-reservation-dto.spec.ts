import { NewReservationDTO } from './new-reservation-dto';

describe('NewReservationDTO', () => {
  it('should create an instance', () => {
    expect(new NewReservationDTO()).toBeTruthy();
  });
});
