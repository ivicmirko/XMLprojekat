import { AccommodationUnitDTO } from './accommodation-unit-dto';

describe('AccommodationUnitDTO', () => {
  it('should create an instance', () => {
    expect(new AccommodationUnitDTO()).toBeTruthy();
  });
});
